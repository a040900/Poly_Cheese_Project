# 🧀 CheeseDog 專案進度記錄

**最後更新**: 2026-02-17 23:50 (台灣時間)  
**專案版本**: 2.0.0  
**當前階段**: Phase 2 - 智能學習與架構優化  

---

## 📍 當前狀態

### ✅ Phase 1 已完成 (v1.0.0)
- **數據獲取**: Binance WebSocket/REST, Polymarket WebSocket/REST, Chainlink 鏈上價格
- **指標系統**: 9 種技術指標 (EMA, OBI, MACD, CVD, HA, VWAP, RSI, POC, Walls)
- **信號引擎**: 綜合趨勢評分系統，支援 3 種交易模式 (積極/平衡/保守)
- **模擬交易**: 虛擬帳戶、自動結算、PnL 追蹤
- **Web Dashboard**: 實時數據推播、明暗主題、交易記錄顯示
- **VPS 部署**: 透過 Tailscale 反向代理成功部署 (`/polycheese` → port 8888)

### 🚀 Phase 2 進行中 (v2.0.0)

#### ✅ 已完成
1. **步驟 9: 手續費模型精準化** (2026-02-17)
   - 創建 `backend/app/strategy/fees.py` 獨立手續費模組
   - 實作 Polymarket 15m 市場浮動費率 (借鏡 NautilusTrader)
     - Buy 端: 0.2% - 1.6% (從 Token 扣除)
     - Sell 端: 0.8% - 3.7% (從 USDC 扣除)
   - 費率根據合約價格動態計算 (越極端價格費率越高)
   - 模擬器已整合新手續費模型

2. **CPU 效能優化** (2026-02-17)
   - 訂單簿輪詢間隔: 2 秒 → 5 秒
   - 交易數據緩衝: 5000 筆 → 2000 筆
   - 預期 CPU 使用率從 50% 降至 30% 以下

3. **UI Bug 修復**
   - 反向代理路徑問題 (CSS/JS 相對路徑)
   - WebSocket/API 自動偵測 sub-path
   - BTC 卡片「連線中...」卡住問題 → 顯示「Binance 即時」
   - 交易記錄列表渲染 (未平倉/已結算，含盈虧和狀態)

4. **步驟 10: 元件狀態機** ✨ (2026-02-17)
   - 新增 `backend/app/core/state.py`
   - `ComponentState` 枚舉: INITIALIZING → READY → RUNNING → STOPPED → DEGRADED/FAULTED
   - `Component` 基類: 帶合法狀態轉換檢查的生命週期管理
   - 所有 DataFeed (Binance/Polymarket/Chainlink) 已繼承 Component
   - Dashboard API 已加入 `component_state` 欄位
   - 新增 `/api/components` 端點查詢所有元件健康度

5. **步驟 11: 事件驅動 MessageBus** ⭐ (2026-02-17)
   - 新增 `backend/app/core/event_bus.py`
   - 輕量級 async Pub/Sub MessageBus，支援 sync/async handler
   - 全域單例 `bus`，整個系統共用一條匯流排
   - **事件主題定義**:
     - `binance.trade` — 每筆成交
     - `binance.kline` — K 線更新/收盤
     - `binance.orderbook` — 訂單簿更新
     - `polymarket.price` — 合約價格更新
     - `chainlink.price` — 鏈上價格更新
     - `signal.generated` — 新交易信號產生
   - **信號引擎改為事件驅動**: 訂閱 `binance.kline` + `binance.orderbook`，收到事件立即計算
   - 內建節流機制 (最少 2 秒間隔，避免暴喝 CPU)
   - **移除舊版 `signal_loop()` 的 10 秒輪詢**
   - 新增 `/api/bus/stats` 端點查詢事件統計
   - 內建佇列大小上限 (10000) + 統計 (已發佈/已處理/錯誤數)

6. **步驟 12: 績效追蹤 + 回測引擎** 📊 (2026-02-17)
   - 新增 `backend/app/performance/tracker.py`
     - `PerformanceTracker` 即時追蹤：勝率、最大回撤、夏普比率、收益因子、期望值
     - 按交易模式 (aggressive/balanced/conservative) 分組統計
     - 權益曲線追蹤
   - 新增 `backend/app/performance/backtester.py`
     - `Backtester` 歷史回測引擎：從 DB 讀取 market_snapshots 重播策略
     - 含 Polymarket 浮動手續費模擬
     - `run_backtest()` 單模式回測快捷函數
     - `run_mode_comparison()` 三模式比較
     - 15 分鐘自動結算邏輯
   - 新增 API 端點:
     - `GET /api/performance` — 即時績效報告
     - `POST /api/backtest` — 執行回測 (可指定模式、資金、手續費)
     - `POST /api/backtest/compare` — 三模式績效比較

7. **步驟 13: LLM 智能整合** 🤖 (2026-02-18)
   - 新增 `backend/app/llm/prompt_builder.py`
     - `PromptBuilder` 結構化提示生成器
     - `build_context_snapshot()` — 將市場/信號/績效/設定打包成結構化 JSON
     - `build_analysis_prompt()` — 支援 4 種分析焦點 (general/signal/risk/mode_switch)
     - `build_param_tune_prompt()` — 參數調優提示（含回測結果比較表）
   - 新增 `backend/app/llm/advisor.py`
     - `LLMAdvisor` 建議處理器
     - 嚴格驗證 AI 建議 JSON 格式
     - 支援自動/手動應用建議：模式切換 + 指標權重調整
     - 安全機制：權重值限制在 1-20，防止 AI hallucination
     - 所有建議記錄到 DB `llm_advices` 表 + MessageBus 事件
   - 新增 API 端點:
     - `GET /api/llm/context` — 結構化系統上下文 (AI 讀取用)
     - `GET /api/llm/prompt?focus=general` — 產生分析 prompt
     - `POST /api/llm/advice` — 接收 AI 建議
     - `POST /api/llm/apply` — 手動應用建議
     - `GET /api/llm/stats` — 建議處理統計
     - `GET /api/llm/history` — 建議歷史

8. **步驟 14: Dashboard 增強** 🎨 (2026-02-18)
   - 前端新增 **Tab 切換式擴充面板區**，4 個分頁：
   - **📊 績效報告 Tab**:
     - 6 個 KPI 卡片 (勝率/收益因子/夏普/最大回撤/總 PnL/手續費)
     - Canvas 權益曲線圖 (含漸變填充+末端圓點)
     - 模式分組統計表格 (按 aggressive/balanced/conservative 分開)
   - **🔬 回測引擎 Tab**:
     - 表單控制：交易模式 / 初始資金 / 快照上限 / 手續費開關
     - 一鍵執行回測，結果即時渲染（含權益曲線）
     - 三模式比較 — 表格 + 🏆 最佳模式標記
   - **🤖 AI 建議 Tab**:
     - 一鍵取得系統上下文 JSON (可複製)
     - 產生分析 Prompt (4 種焦點 + 一鍵複製)
     - AI 建議歷史列表 (卡片式，含行動/推薦/信心/風險/套用狀態)
     - LLM 統計橫條
   - **🩺 元件健康 Tab**:
     - 3 張元件健康卡片 (Binance/Polymarket/Chainlink)
     - 狀態底色條 (running=綠/ready=藍/stopped=紅/degraded=黃)
     - MessageBus 統計 (已發佈/已處理/錯誤/佇列)
   - **新增後端 API**:
     - `GET /api/components` — 元件健康狀態
     - `GET /api/bus/stats` — MessageBus 統計
   - CSS 新增 ~560 行樣式 (Tab 系統/KPI/回測/AI/健康/響應式)
   - JS 新增 ~490 行邏輯 (Tab 切換/資料抓取/圖表渲染/互動)

9. **額外修正: 反向代理子路徑支援** 🔧 (2026-02-18)
   - 解決 Tailscale Funnel / 反向代理部署到子路徑 (如 `/cheesedog`) 時，CSS/JS 無法載入的問題。
   - `backend/app/config.py`: 新增 `ROOT_PATH` 環境變數。
   - `backend/app/main.py`: FastAPI 動態注入 `<base>` 標籤到 HTML。
   - `frontend/js/app.js`: 優先從 `<base>` 標籤讀取 API/WS 基礎路徑。


#### ✅ Phase 2 全部完成！

---

## 🗂️ 專案結構概覽

```
cheeseproject/
├── backend/
│   ├── app/
│   │   ├── main.py              # FastAPI 主程式 (已整合 MessageBus)
│   │   ├── config.py            # 全域設定 (已調整輪詢間隔)
│   │   ├── database.py          # SQLite 資料庫
│   │   ├── core/                # ✨ Phase 2 新增：核心模組
│   │   │   ├── state.py         # ComponentState 狀態機 + Component 基類
│   │   │   └── event_bus.py     # MessageBus 事件匯流排 (Pub/Sub)
│   │   ├── data_feeds/          # Binance, Polymarket, Chainlink (已繼承 Component)
│   │   ├── indicators/          # 9 種技術指標
│   │   ├── strategy/
│   │   │   ├── signal_generator.py  # 信號生成引擎 (已改為事件驅動觸發)
│   │   │   └── fees.py          # 手續費模組
│   │   ├── trading/
│   │   │   └── simulator.py    # 模擬交易引擎 (已整合新手續費)
│   │   ├── security/
│   │   │   └── password_manager.py  # 隨機密碼驗證
│   │   └── api/                # API 路由
│   ├── tests/
│   │   ├── test_data_feeds.py  # 數據源驗證腳本
│   │   └── check_status.py     # 系統狀態檢查腳本
│   └── requirements.txt
├── frontend/
│   ├── index.html              # Dashboard UI
│   ├── css/style.css           # 雙主題樣式
│   └── js/app.js               # WebSocket + 渲染邏輯 (已修復路徑問題)
├── data/
│   └── cheesedog.db            # SQLite 數據庫
├── .env.example
├── IMPLEMENTATION_PLAN.md      # 完整實施計畫 (Phase 1-3)
└── README.md
```

---

## 🔧 技術架構要點

### 數據流 (Phase 2 事件驅動)
1. **WebSocket 實時推播** (每 5 秒)
   - Binance 交易流 + K 線
   - Polymarket UP/DOWN 合約價格
   - 訂單簿輪詢 (REST, 每 5 秒)

2. **信號生成** (Phase 2: 事件驅動 ✨)
   - 訂閱 `binance.kline` + `binance.orderbook` 事件
   - 收到事件後立即計算（節流 2 秒）
   - 計算 9 種指標
   - 綜合趨勢評分 (-100 ~ +100)
   - 發佈 `signal.generated` 事件

3. **模擬交易**
   - 開倉: Buy 端手續費 (0.2%-1.6%)
   - 結算: Sell 端手續費 (0.8%-3.7%)
   - 15 分鐘自動結算

### 事件匯流排架構 (Phase 2 新增)
```
[BinanceFeed]      → binance.trade / binance.kline / binance.orderbook
[PolymarketFeed]   → polymarket.price
[ChainlinkFeed]    → chainlink.price
                          ↓ (MessageBus)
[on_market_data_event]   → signal.generated
                          ↓
[SimulationEngine]       → 自動交易
[Dashboard]              → 推播給前端
```

### 部署環境
- **VPS**: Tailscale 反向代理
- **路徑映射**: `/polycheese` → `localhost:8888`
- **前端**: 相對路徑 + 動態 base path 偵測
- **後端**: FastAPI + Uvicorn

---

## 📊 效能指標

| 項目 | Phase 1 | Phase 2 (優化後) |
|------|---------|-----------------|
| CPU 使用率 | ~50% | 預期 <20% |
| 訂單簿輪詢 | 2 秒 | 5 秒 |
| 交易緩衝 | 5000 筆 | 2000 筆 |
| 信號延遲 | 10 秒 (輪詢) | ~2 秒 (事件驅動 + 節流) |

---

## 🛠️ 關鍵設定檔

### `.env` 環境變數 (重要)
```bash
# Polymarket 市場選擇
POLYMARKET_SERIES_SLUG=btc-up-or-down-15m
POLYMARKET_AUTO_SELECT_LATEST=true

# Polygon RPC (Chainlink 數據源)
POLYGON_RPC_URL=https://polygon-rpc.com

# 模擬帳戶初始資金
SIM_INITIAL_BALANCE=1000.0
```

### 手續費設定 (`config.py`)
```python
# Phase 2 浮動手續費
PM_FEE_BUY_RANGE = (0.002, 0.016)    # Buy: 0.2%-1.6%
PM_FEE_SELL_RANGE = (0.008, 0.037)   # Sell: 0.8%-3.7%
PM_FEE_BUY_DEFAULT = 0.005           # 預設 Buy: 0.5%
PM_FEE_SELL_DEFAULT = 0.015          # 預設 Sell: 1.5%
```

---

## 🐛 已知問題與解決

### ✅ 已解決
1. **反向代理樣式失效**
   - 問題: `/polycheese` 下 `/static/css/style.css` 404
   - 解決: 改為相對路徑 `static/css/style.css` + 動態 WebSocket URL

2. **BTC 卡片「連線中...」卡住**
   - 問題: `val-btc-change` 元素未更新
   - 解決: `renderMarket()` 加入更新邏輯

3. **交易記錄顯示「暫無交易記錄」**
   - 問題: 後端未推播 `recent_trades`
   - 解決: 新增 `get_recent_trades()` + 前端 `renderRecentTrades()`

### 🔍 待觀察
- CPU 使用率是否降至 <20% (事件驅動 + 節流)
- MessageBus 佇列使用率
- 手續費模型對 PnL 的影響

---

## 📋 下次繼續的起點

### 建議優先級
1. **步驟 12: 回測引擎** ⭐⭐⭐
   - 驗證策略有效性
   - 手續費影響分析
   - 參數優化
   - 利用 MessageBus 重播歷史事件

2. **步驟 13: LLM 整合**
   - 結構化 prompt 生成
   - AI 代理模式建議端點

3. **步驟 14: Dashboard 增強**
   - 績效圖表
   - 元件健康度面板 (利用新 component_state)
   - 指標貢獻度視覺化
   - 事件匯流排統計面板

### 快速啟動指令
```bash
# VPS 更新代碼
cd cheeseproject
git pull

# 本機開發
cd backend
python -m app.main

# 測試連通性
python tests/test_data_feeds.py

# 檢查系統狀態
python tests/check_status.py
```

### 新增 API 端點 (Phase 2)
```bash
# 查詢元件健康度
GET /api/components

# 查詢 MessageBus 統計
GET /api/bus/stats
```

---

## 🔗 重要參考資料

1. **NautilusTrader 架構**
   - Polymarket 整合文件: 15m 市場手續費結構
   - ComponentState 狀態機設計 → ✅ 已實作
   - MessageBus Pub/Sub 模式 → ✅ 已實作
   - 策略介面統一 (回測/實盤零修改)

2. **Repository**
   - GitHub: `a040900/Poly_Cheese_Project`
   - 分支: `main`

---

## 💬 對話摘要 (本次會話)

### 主要成就
1. 步驟 10: ComponentState 狀態機完成
2. 步驟 11: MessageBus 事件匯流排完成
3. 信號引擎從 10 秒輪詢改為事件驅動（預估延遲 10s → 2s）
4. 所有 DataFeed 繼承 Component 基類，具備健康度追蹤

### 技術決策
- **事件驅動 + 節流**: 訂閱 `binance.kline` + `binance.orderbook`，收到事件立即計算但加 2 秒節流
- **不訂閱 `binance.trade`**: 避免高頻事件導致過度計算
- **保留向後相容**: 舊的 `set_update_callback` 機制仍可用
- **全域單例 bus**: 簡化依賴注入，全系統共用一條匯流排

---

**下次對話時，請參考此文件快速回到進度！**
